{
  // Typecheck for CI. Same scope as the production build - see
  // `tsconfig.build.json` for why specs are out of it - with the incremental
  // cache off, since a throwaway runner has nowhere to keep a tsbuildinfo.
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "incremental": false
  }
}
